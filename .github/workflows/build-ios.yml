name: Build iOS

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    continue-on-error: true
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable
    
    - name: Install xcodegen
      run: |
        brew install xcodegen || echo "xcodegen installation failed, will try alternative"
    
    - name: Generate Xcode Project
      continue-on-error: true
      run: |
        cd ${{ github.workspace }}
        if [ -f "project.yml" ]; then
          echo "📦 Generating Xcode project with xcodegen..."
          xcodegen generate || {
            echo "⚠️ xcodegen failed, checking if project already exists..."
            if [ -f "VSSHKR.xcodeproj/project.pbxproj" ]; then
              echo "✅ Project already exists"
            else
              echo "❌ Project generation failed"
              exit 1
            fi
          }
          echo "✅ Xcode project generated or already exists"
        else
          echo "⚠️ project.yml not found, skipping project generation"
        fi
    
    - name: Check if project exists
      id: check_project
      run: |
        cd ${{ github.workspace }}
        if [ -f "VSSHKR.xcodeproj/project.pbxproj" ]; then
          echo "project_exists=true" >> $GITHUB_OUTPUT
          echo "✅ Project found"
        else
          echo "project_exists=false" >> $GITHUB_OUTPUT
          echo "ℹ️ Project not found - will show info message"
        fi
    
    - name: Install CocoaPods
      if: steps.check_project.outputs.project_exists == 'true'
      run: |
        sudo gem install cocoapods
        pod --version
    
    - name: Install Dependencies
      if: steps.check_project.outputs.project_exists == 'true'
      run: |
        cd ${{ github.workspace }}
        if [ -f "Podfile" ]; then
          echo "📦 Installing CocoaPods dependencies..."
          echo "Podfile contents:"
          cat Podfile
          echo ""
          echo "Current directory contents:"
          ls -la | head -20
          echo ""
          echo "Checking if project exists:"
          ls -la VSSHKR.xcodeproj/ 2>/dev/null | head -5 || echo "Project not found"
          echo ""
          echo "Running pod install with full output..."
          set -x  # Enable command tracing
          pod install --verbose 2>&1 | tee pod_install.log
          POD_EXIT_CODE=${PIPESTATUS[0]}
          set +x  # Disable command tracing
          echo ""
          echo "Pod install exit code: $POD_EXIT_CODE"
          echo ""
          if [ $POD_EXIT_CODE -ne 0 ]; then
            echo "❌ pod install failed with exit code $POD_EXIT_CODE"
            echo "Full pod install log:"
            cat pod_install.log
            echo ""
            echo "Checking what was created:"
            ls -la | grep -E "(Pods|workspace|Podfile)"
            exit 1
          fi
          echo "✅ pod install completed"
          echo ""
          echo "📁 Verifying workspace was created..."
          if [ -f "VSSHKR.xcworkspace/contents.xcworkspacedata" ]; then
            echo "✅ Workspace created successfully"
            ls -la VSSHKR.xcworkspace/
          else
            echo "⚠️ Workspace not found at VSSHKR.xcworkspace"
            echo "Looking for any workspace files:"
            find . -name "*.xcworkspace" -type d 2>/dev/null
            echo ""
            echo "Checking Pods directory:"
            if [ -d "Pods" ]; then
              echo "✅ Pods directory exists"
              ls -la Pods/ | head -10
            else
              echo "❌ Pods directory not found"
            fi
            echo ""
            echo "Checking Podfile.lock:"
            if [ -f "Podfile.lock" ]; then
              echo "✅ Podfile.lock exists"
              cat Podfile.lock
            else
              echo "❌ Podfile.lock not found"
            fi
            echo ""
            echo "❌ Workspace not found - this will cause archive to fail"
            exit 1
          fi
        else
          echo "⚠️ Podfile not found"
        fi
    
    - name: Check final project type
      id: check_final_project
      if: steps.check_project.outputs.project_exists == 'true'
      run: |
        cd ${{ github.workspace }}
        if [ -f "VSSHKR.xcworkspace/contents.xcworkspacedata" ]; then
          echo "project_type=workspace" >> $GITHUB_OUTPUT
          echo "✅ Workspace found (after pod install)"
        elif [ -f "VSSHKR.xcodeproj/project.pbxproj" ]; then
          echo "project_type=project" >> $GITHUB_OUTPUT
          echo "✅ Project found (no workspace, using project)"
        else
          echo "project_type=none" >> $GITHUB_OUTPUT
          echo "❌ No project or workspace found"
        fi
    
    - name: Build iOS App (for device)
      if: steps.check_final_project.outputs.project_type != 'none'
      continue-on-error: true
      run: |
        cd ${{ github.workspace }}
        echo "🔨 Building for iOS device (required for IPA)..."
        if [ "${{ steps.check_final_project.outputs.project_type }}" == "workspace" ]; then
          xcodebuild clean build \
            -workspace VSSHKR.xcworkspace \
            -scheme VSSHKR \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -allowProvisioningUpdates 2>&1 | tee build.log || {
              echo "❌ Build failed. Last 50 lines of log:"
              tail -50 build.log
              exit 1
            }
        elif [ -f "VSSHKR.xcodeproj/project.pbxproj" ]; then
          xcodebuild clean build \
            -project VSSHKR.xcodeproj \
            -scheme VSSHKR \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -allowProvisioningUpdates 2>&1 | tee build.log || {
              echo "❌ Build failed. Last 50 lines of log:"
              tail -50 build.log
              exit 1
            }
        fi
        echo "✅ Build completed successfully"
    
    - name: Archive iOS App
      if: steps.check_final_project.outputs.project_type != 'none'
      run: |
        cd ${{ github.workspace }}
        echo "📦 Creating archive..."
        mkdir -p ./build
        
        # Force use workspace if it exists (required for CocoaPods)
        if [ -f "VSSHKR.xcworkspace/contents.xcworkspacedata" ]; then
          echo "✅ Using workspace (required for CocoaPods dependencies)"
          xcodebuild archive \
            -workspace VSSHKR.xcworkspace \
            -scheme VSSHKR \
            -archivePath ./build/VSSHKR.xcarchive \
            -sdk iphoneos \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            -allowProvisioningUpdates 2>&1 | tee archive.log || {
              echo "❌ Archive failed. Last 50 lines:"
              tail -50 archive.log
              exit 1
            }
        elif [ -f "VSSHKR.xcodeproj/project.pbxproj" ]; then
          echo "❌ Workspace not found but Podfile exists!"
          echo "❌ Cannot archive without workspace when using CocoaPods"
          echo "❌ This means pod install failed or didn't create workspace"
          echo ""
          echo "Checking for Podfile:"
          ls -la Podfile 2>/dev/null || echo "Podfile not found"
          echo ""
          echo "Checking for workspace:"
          ls -la *.xcworkspace 2>/dev/null || echo "No workspace found"
          exit 1
        else
          echo "❌ No project or workspace found"
          exit 1
        fi
        # Verify archive was created
        if [ -d "./build/VSSHKR.xcarchive" ]; then
          echo "✅ Archive created successfully at ./build/VSSHKR.xcarchive"
          ls -lh ./build/VSSHKR.xcarchive
        else
          echo "❌ Archive file not found after xcodebuild archive"
          echo "Contents of build/ directory:"
          ls -la ./build/ 2>/dev/null || echo "build/ directory is empty or doesn't exist"
          exit 1
        fi
    
    - name: Create IPA manually (skip exportArchive)
      if: steps.check_final_project.outputs.project_type != 'none'
      run: |
        cd ${{ github.workspace }}
        echo "📤 Creating IPA manually from archive..."
        if [ -d "./build/VSSHKR.xcarchive" ]; then
          # Find the .app inside the archive
          APP_PATH=$(find ./build/VSSHKR.xcarchive -name "VSSHKR.app" -type d | head -1)
          if [ -n "$APP_PATH" ] && [ -d "$APP_PATH" ]; then
            echo "✅ Found .app at: $APP_PATH"
            mkdir -p ./build/Payload
            cp -r "$APP_PATH" ./build/Payload/
            cd ./build
            echo "📦 Zipping into IPA..."
            zip -r -q VSSHKR.ipa Payload/
            cd ..
            if [ -f "./build/VSSHKR.ipa" ]; then
              echo "✅ IPA created successfully: ./build/VSSHKR.ipa"
              ls -lh ./build/VSSHKR.ipa
            else
              echo "❌ Failed to create IPA"
              exit 1
            fi
          else
            echo "❌ .app not found in archive"
            echo "Searching in archive structure:"
            find ./build/VSSHKR.xcarchive -type d -name "*.app" || echo "No .app found"
            exit 1
          fi
        else
          echo "❌ Archive not found at ./build/VSSHKR.xcarchive"
          echo "Available files in build/:"
          ls -la ./build/ 2>/dev/null || echo "build/ directory does not exist"
          exit 1
        fi
    
    - name: Upload Artifacts
      if: steps.check_final_project.outputs.project_type != 'none'
      continue-on-error: true
      uses: actions/upload-artifact@v4
      with:
        name: iOS-Build
        path: |
          build/
          VSSHKR.xcworkspace
          VSSHKR.xcodeproj
    
    - name: Info - Project not found
      if: steps.check_project.outputs.project_exists == 'false'
      run: |
        echo "════════════════════════════════════════"
        echo "ℹ️ Xcode project not found"
        echo "════════════════════════════════════════"
        echo ""
        echo "El workflow intentó generar el proyecto automáticamente"
        echo "pero no se pudo crear. Esto puede deberse a:"
        echo ""
        echo "- xcodegen no se instaló correctamente"
        echo "- project.yml tiene algún error"
        echo "- Falta alguna configuración"
        echo ""
        echo "El workflow completará exitosamente pero sin compilar."
        echo "Revisa los logs arriba para ver el error específico."
